{
  "name": "piano-interval-filter",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time piano interval detection with coincident partial filtering for aural tuning training",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
